"""Shared data/feature/evaluation code for the marketing-mix-modeling notebooks.

Every notebook imports from this package instead of redefining adstock,
saturation, the train/test split, or the metrics -- so all models are
compared on an identical, shared feature pipeline (see README:
"Methodology").
"""
